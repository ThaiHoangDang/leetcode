#include <vector>
using namespace std;

// approach:
// time:
// space:

class Solution {
public:
    vector<vector<int>> permute(vector<int>& nums) {
        vector<vector<int>> result;

        

        return result;
    }
};